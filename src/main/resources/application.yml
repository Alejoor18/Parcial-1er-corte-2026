server:
  port: ${PORT:8080}

security:
  jwt:
    secret: ${JWT_SECRET:clave-super-segura-de-minimo-32-caracteres-123456}
    expiration-millis: 3600000

spring:
  application:
    name: gafas-api
  r2dbc:
    url: ${SPRING_R2DBC_URL:r2dbc:postgresql://localhost:5432/gafasdb}
    username: ${SPRING_R2DBC_USERNAME:postgres}
    password: ${SPRING_R2DBC_PASSWORD:postgres}

logging:
  level:
    root: INFO